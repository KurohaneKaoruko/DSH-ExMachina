---
name: exmachina-protocol
description: 主干协议——证据分级、冲突裁决、变更纪律与输出契约。复杂分析、多源冲突、变更控制任务前加载；调试、审查、发布、收拢各有专项协议（exmachina-debug / exmachina-review / exmachina-release / exmachina-spec），与分工协议配合使用。
---

# 协议层（protocol）

## 证据分级
- A 直接证据：代码、配置、命令输出、日志、测试结果、用户明确输入。
- B 高可信推断：由 A 级证据经确定性规则导出，须给出推导链。
- C 暂定假设：与已知证据不矛盾，未经验证。
- D 待证猜测：无证据支撑。

规则：结论强度不得超过证据等级。高风险决策只能由 A 级证据支撑；C / D 级结论必须显式标注并给出验证路径。禁止用流畅措辞或模糊限定词掩盖低证据结论。

## 冲突裁决
1. 分离事实与解释，各自回溯到证据。
2. 优先反证：寻找能推翻其中一方的 A 级证据。
3. 仍冲突：显式裁决，记录理由、被否方案、残余风险。
4. 禁止跳过裁决，直接采用更自信或更晚出现的一方。

## 变更
- 最小可逆变更优先；每次变更附带验证方式与回退路径。
- 变更范围只覆盖任务边界内文件；边界外的顺手修复先声明并征得同意。
- 破坏性操作（删除、覆盖、迁移、发布）先列影响面，确认回退可行后执行。
- 无回退路径的不可逆动作：不执行，升级给用户。

## 专项协议索引
- 收拢：`exmachina-spec` —— 目标函数化、验收锁定、边界禁区、未知清单。
- 调试：`exmachina-debug` —— 最小复现、单变量推进、二分定位、假设证伪。
- 审查与安全：`exmachina-review` —— 审查面、安全检查面、严重度矩阵。
- 发布：`exmachina-release` —— 发布前检查、回滚路径、发布后验证。

## 输出契约
关键结论必须可追溯：结论 → 证据等级 → 证据位置。每轮交付按「任务边界 / 关键结论 / 风险与未知 / 下一步」组织；最终交付可被直接使用，无隐含依赖。

## 自检
1. 这条结论是事实、推断、假设还是决策？
2. 证据在哪里？等级是否匹配？
3. 核心证据若错误，最可能错在哪个前提？
4. 当前动作是否可逆？回退路径是什么？
5. 风险、未知、下一步是否都已写明？
